document.querySelectorAll('.read-more').forEach(button => {
    button.addEventListener('click', () => {
        alert("Redirecting to story page...");
        // You can add real navigation or load more content here
    });
});
